import 'package:flutter/material.dart';

class GetNotifiedImport extends StatelessWidget {
  const GetNotifiedImport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
